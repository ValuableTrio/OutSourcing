spring:
  datasource:
    url: jdbc:mysql://localhost:3306/outsourcing
    username: "root"
    password: "0000"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
       ddl-auto: create
    properties:
       hibernate:
         format_sql: true


logging:
  level:
    org.hibernate.SQL: debug